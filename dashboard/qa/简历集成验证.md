# 简历管理集成验证

入口：原工作台 `dashboard.html`，原服务端口 8420。新增功能直接嵌入侧边栏，不创建新工作台。

自动化检查：`node dashboard/qa/verify-resume-integration.cjs`。

检查使用独立浏览器上下文、端口 8421 与系统临时文件夹，不修改用户浏览器记录或真实简历。运行后关闭测试服务并删除临时简历文件。

已通过：

- 原有投递页面、统计和记录保持可用。
- 原页面切换简历管理，无简历优化入口。
- 多文件上传、同名不同版本、错误文件不阻断后续上传、内容重复跳过。
- 刷新及重启服务后简历仍存在。
- PDF 查看和下载；下载保留原文件名及完整字节。
- 新建岗位选择已上传 PDF，岗位列表可直接查看绑定文件。
- 编辑其他字段时保留绑定；简历重命名保留历史投递名称。
- 删除前确认；删除 PDF 不删除岗位，历史岗位仍能正常编辑。
- 桌面和 390px 手机布局检查，无页面水平溢出。
- 跨站读写、无凭据写入、直接访问私有文件与目录越界受到限制。
- 无 JavaScript 运行错误。

机器结果保存在 `qa/resume-integration/results.json`；截图为测试数据，未放入代码版本。
